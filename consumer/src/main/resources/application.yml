server:
  port: 9000

spring:
  application:
    name: consumer

dubbo:
  application:
    name: dubbo-consumer
  registry:
    address: zookeeper://127.0.0.1:2181?backup=127.0.0.1:2182,127.0.0.1:2183
  protocol:
    name: dubbo # dubbo 协议
    port: 20880 # 开放端口
  consumer:
    check: false